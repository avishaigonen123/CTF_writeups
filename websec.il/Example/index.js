const path = require("path")
const bla = '../../../../../../../usr/src/app/views'
const outputFilePath = path.join(__dirname, 'uploads', bla);

console.log(`File will be saved to: ${outputFilePath}`);